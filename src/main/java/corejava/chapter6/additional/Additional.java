package corejava.chapter6.additional;

public class Additional {

    // + проверить стирание для ? super (во что стирается) //нельзя писать super
    // + как кастить дженерик правильно (с примером) //из лекций
    // + принимает поле и возвращает его класс или класс границу(во что скастится)
    // + boolean contains(Object o); PECS как понять по определению метода(сигнатуре)
    // + 17 (проверено)
    // + какой toArray() использовать и почему с примером (19)

    public static void main(String[] args) {

    }
}
